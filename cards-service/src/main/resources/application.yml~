server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eazybank
    username: root
    password: ghosh

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


